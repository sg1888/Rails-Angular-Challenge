railsAngularTestApp.controller 'ApplicationCtrl', ['$scope', ($scope) ->
  # Like the Rails way, we should have a top-based controller and other controllers will be inherited from this controller
  return
]
